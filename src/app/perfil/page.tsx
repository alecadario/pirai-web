'use client';
import AppShell from '@/components/layout/AppShell';

export default function Page() {
  return (
    <AppShell>
      <div className="p-8">
        <h1 className="text-2xl font-bold text-[var(--color-brand-dark)] mb-2">Perfil</h1>
        <p className="text-[var(--color-brand-muted)] text-sm">Próximamente — en desarrollo.</p>
      </div>
    </AppShell>
  );
}
